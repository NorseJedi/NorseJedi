#!/bin/bash

branch_name=$(git branch | grep "*" | sed "s/\* //")

if [[ $branch_name == "dev" ]]; then
  git commit -aqm "commit"
  git checkout main
  git merge dev -qm "merge" 
  git commit -aqm "commit" 
  git push -q
  git checkout dev
else
  echo "Not in dev branch, aborting."
fi
